div.blackstripe
div.title-banner
  a.title-link(href='/') COMPAGNIE OTHELLO
.navbar.navbar-light.navbar-expand-lg
  .container
    //- a.navbar-brand(href='/')
    //-   i.fas.fa-cube
    //-   | Project name
    button.navbar-toggler(type='button', data-toggle='collapse', data-target='.navbar-collapse')
      span.sr-only Toggle navigation
      span.navbar-toggler-icon
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li.nav-item-header(class=(title === 'Home') ? 'active' : undefined)
          a.nav-link-header(href='/') Actualités
        //- li.nav-item-header(class=(title === 'API Examples') ? 'active' : undefined)
        //-   a.nav-link-header(href='/api') Agenda
        li.nav-item-header(class=(title === 'Calendar') ? 'active' : undefined)
          a.nav-link-header(href='/calendar') Agenda  
        li.nav-item-header(class=(title === 'Contact') ? 'active' : undefined)
          a.nav-link-header(href='/contact') Activités 
          //- dropdown
        li.nav-item-header(class=(title === 'Status') ? 'active' : undefined)
          a.nav-link-header(href='/status') Cours
          //- dropdown
        li.nav-item-header(class=(title === 'Status') ? 'active' : undefined)
          a.nav-link-header(href='/status') Coaching
          //- dropdown
        li.nav-item-header(class=(title === 'Status') ? 'active' : undefined)
          a.nav-link-header(href='/status') Notre équipe    
      ul.nav.navbar-nav.ml-auto
        if !user
          li.nav-item-header(class=(title === 'Login') ? 'active' : undefined)
            a.nav-link-header(href='/login') Login
          li.nav-item-header(class=(title === 'Create Account') ? 'active' : undefined)
            a.nav-link-header(href='/signup') Create Account
        else
          li.dropdown(class=(title === 'Account Management') ? 'active' : undefined).nav-item-header
            a.dropdown-toggle.nav-link-header(href='#', data-toggle='dropdown')
              if user.profile.picture
                img(src=user.profile.picture)
              else
                img(src=user.gravatar(60))
              span= user.profile.name || user.email || user.id
              i.caret
            ul.dropdown-menu
              li.dropdown-item
                a(href='/account') My Account
              li.divider.dropdown-item
              li.dropdown-item
                a(href='/logout') Logout
